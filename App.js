import React from 'react';
import type { Node } from 'react';
import { SafeAreaView, View } from 'react-native';

const App: () => Node = () => {
  return (
    <SafeAreaView>
      <View />
    </SafeAreaView>
  );
};

export default App;
